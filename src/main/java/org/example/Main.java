package org.example;

// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    public static void main(String[] args) {
//        String customMessage = System.getenv("CUSTOM_MESSAGE");
//
//        if (customMessage != null) {
//            System.out.println("Message with environment variable: " + customMessage);
//        } else {
//            System.out.println("Environment variable CUSTOM_MESSAGE not set.");
//        }
    }
}